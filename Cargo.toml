[profile.release]
opt-level = 3
overflow-checks = true
lto = true

[workspace]
members = [
    "exchange",
    "lib/flv",
    "lib/rtmp",
    "lib/balance",
    "lib/transport",
    "lib/configure",
    "publish",
    "pull"
]
