[profile.release]
opt-level = 3
overflow-checks = true
lto = true

[workspace]
members = [
    "balance",
    "exchange",
    "lib/flv",
    "lib/rtmp",
    "lib/transport",
    "lib/configure",
    "publish",
    "pull"
]
