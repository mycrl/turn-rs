[profile.release]
opt-level = 3
overflow-checks = true
lto = true

[workspace]
members = [
    "lib/codec",
    "lib/golomb",
    "lib/balance",
    "lib/transport",
    "lib/configure",
    "lib/multiplexer",
    "exchange",
    "publish",
    "pull"
]
