#include <stdio.h>

#define unsigned long long int u64

struct Buckets {
    u64 buckets[256]
}

u64 find_from_x86() {
    
}

int main() {
    buckets = (u64*)malloc(256 * sizeof(u64));
    
    printf("size %", sizeof(num));
    return 0;
}