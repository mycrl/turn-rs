{
    "name": "turn-rs",
    "workspaces": [
        "./dashboard",
        "./turn-napi",
        "./turn-napi/examples"
    ],
    "scripts": {
        "build-napi": "npm run build --workspace=turn",
        "build-napi-dev": "npm run build-dev --workspace=turn",
        "build-dashboard": "npm run build --workspace=dashboard",
        "dev-dashboard": "npm run dev --workspace=dashboard",
        "napi-example": "npm run example --workspace=examples --"
    }
}
