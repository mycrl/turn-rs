import grpc from '@grpc/grpc-js'
import loader from '@grpc/proto-loader'

// grpc.loadPackageDefinition(loader.loadSync())

export class Balance {
    constructor() {

    }
}

export function createBalance() {
    return new Promise((resolve, reject) => {
        
    })
}
