[package]
name = "turn-exports"
version = "0.1.0"
edition = "2021"

[dependencies]
turn-rs = { path = "../turn" }
faster-stun = { path = "../stun" }

[lib]
name = "libturn" 
crate-type = ["staticlib"]
