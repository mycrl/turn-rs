# turn-server-sdk

Rust client SDK for interacting with the `turn-server` gRPC API exposed by the `turn-rs` project. The crate bundles the generated protobuf bindings and helper utilities so applications can consume TURN hooks and control endpoints programmatically.
